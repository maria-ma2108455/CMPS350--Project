'use client'
import React, { useEffect, useState } from 'react';
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';
import styles from "./page.module.css";

export default function Top3Details({itemsdetails}) {
  return (
    <div>Top3Details</div>
  )
}
